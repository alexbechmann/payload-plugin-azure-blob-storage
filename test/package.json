{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "generate-types": "json2ts ./charts/my-chart/values.schema.json > ./test/values-schema.d.ts"
  },
  "author": "Alex",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "vitest": "^0.18.1"
  }
}
