{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "test"
  ],
  "scripts": {
    "postinstall1": "lerna bootstrap",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "dev": "lerna run dev --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.6.2"
  }
}
